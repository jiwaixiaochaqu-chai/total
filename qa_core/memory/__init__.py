"""记忆层：聊天历史、用户反馈和 MySQL 存储基类。

子模块说明：
- base：MySQL 存储基类 _MySqlStore，统一加载项目配置并延迟创建 SQLAlchemy 引擎。
- history：聊天历史持久化和上下文压缩工具。MySQL 负责保存完整会话；模型侧只接收
  "历史摘要 + 最近消息"，避免把全部历史都塞进 Prompt 导致上下文膨胀。
- feedback：用户反馈持久化层。在线链路只记录反馈，不立即改变检索或答案，避免误点
  反馈污染主流程。

设计决策：
- SQLAlchemy 引擎延迟创建，构造对象时不会立即连接 MySQL。
- 使用 pool_pre_ping=True 避免长时间空闲后连接失效导致的请求失败。
- 反馈与历史数据解耦：反馈只记录不干预检索权重，需要人工审核后再进入训练集。

调用顺序：问答历史或反馈存储 -> memory。
"""

