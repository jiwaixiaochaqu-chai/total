# API 限流与调用失败排查

API 调用失败时，应先确认错误码、请求时间、接口路径、调用方应用、并发数和一分钟内请求量。若返回限流类错误，通常说明调用频率超过套餐或接口保护阈值。

高频接口应避免无间隔重试，推荐使用指数退避、异步队列、幂等请求号和失败补偿任务。批量同步类任务应错峰执行，避免在账单结算、报表导出或业务高峰期集中调用。

如果业务确需更高调用额度，应提供峰值 QPS、接口清单、业务用途和历史调用日志，由平台评估是否调整限额。
